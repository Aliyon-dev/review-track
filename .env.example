# Vite dev server proxies /api/* to this target
VITE_API_PROXY_TARGET=http://localhost:8000

# Optional: override API base in production builds (leave empty to use relative /api)
# VITE_API_BASE_URL=
